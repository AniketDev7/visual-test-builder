# Copy to .env. With no key set, VisualFlow runs the AI agents in deterministic
# MOCK mode so the whole app still works for demos.
ANTHROPIC_API_KEY=

# Server port (default 4000)
PORT=4000

# Used by the MCP server to reach the running VisualFlow HTTP server.
VISUALFLOW_URL=http://localhost:4000
